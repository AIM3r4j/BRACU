;General Registers:     
mov ax, ax
mov ax, bx

mov es, ax
mov ds, bx

mov si, ax
mov di, bx

;Segment Register:
mov ax, cs
mov bx, ds

mov si, cs
mov di, ds

;Memory Location:
mov ax, di
mov bx, si

mov es, di
mov ds, si